# 🏫 Primary School Management System — Roadmap v16

> **Stack:** Vue 3 + Vite + Tailwind CSS + Supabase
> **Roles:** Super Admin · Admin/Director · Teacher · Librarian · Parent (anon, link-only)
> **Schema:** v10 (fresh install or migrated from v9)
> **Date:** June 2026

---

## 🧠 Core Design Decisions

### Every User IS a Teacher

In Cambodian schools, all staff members are teachers. The librarian is a teacher who manages the library. The admin/director is a teacher who runs the school. Therefore:

- **Every user account** always has a matching `teachers` row (personal profile)
- The `role` field in `public.users` only controls **what they can access** in the app — not who they are
- There is no such thing as a user without a teacher profile
- `teachers.user_id` is `NOT NULL UNIQUE` — one profile per account, always linked

### User → Teacher Connection

```
auth.users
    ↓ id (cascade delete)
public.users        ← role (super_admin/admin/teacher/librarian), status (active/inactive)
    ↓ user_id (not null, unique)
teachers            ← full_name, gender, dob, phone, degree, address, profile_url
    ↓ id
classes             ← teacher_id → which teacher owns which class
    ↓ id
students            ← class_id → which class each student belongs to
```

### Parent Portal — Link-Only Entry (v9)

Parents no longer search by name + DOB. Access is granted exclusively via a report link generated by the teacher:

- Teacher generates one link per class per month/semester from the **Score Ranking page**
- Link is a UUID-based URL: `/parent/report/:report_link_id`
- Parent opens link → sees a dropdown of all students in that class → selects their child
- Report card shows scores + attendance for that specific month/period only
- Health, growth, vaccinations, sick days are **not exposed** — private to staff only
- Teacher can leave a text or voice message per student
- Parent can reply with text or voice per student
- Anon RLS is scoped to `report_link_id` — no open `using (true)` policies

### Report Link Approval Flow (v10)

Before a teacher can share a parent report link, the principal (admin) must approve it:

- Teacher clicks **"Request Approval"** on `/teacher/scores/ranking` → `report_links.status` set to `pending`
- Admin receives an **in-app notification** (bell icon in topbar) with unread count badge
- Admin opens notification → reviews the request → clicks **Approve** or **Reject** (with optional rejection note)
- Teacher receives an in-app notification of the decision
- If approved → **"Share Link"** button unlocks for the teacher
- If rejected → teacher sees the rejection note and can revise/re-request
- Parent report card footer displays the **principal's digital signature image + school stamp image**
- Signature and stamp images are uploaded by admin in **Settings → School Information**

### Teacher Phrase Library (v10)

Teachers maintain a personal library of reusable feedback phrases for student messages:

- Each teacher owns their own phrases — private, not shared across teachers
- When composing a student message (in report-replies), saved phrases appear as **clickable chips** below the message box
- Clicking a chip **appends** the phrase to the current message text
- Teacher can add a new phrase inline and delete existing ones
- Phrases persist across classes, months, and academic years
- Completely optional — message box works normally without any saved phrases

---

## 📁 Project Folder Structure

```
src/
├── assets/                  # Images, icons, fonts
├── components/
│   ├── honorBoard/          # Honor board editor components
│   │   ├── EditorCanvas.vue
│   │   ├── TemplateGallery.vue
│   │   ├── TextLayerControls.vue
│   │   └── Toolbar.vue
│   ├── report/              # NEW v10 — report-related reusable components
│   │   └── PhrasePicker.vue # Phrase chip picker for teacher message composition
│   └── shared/              # NEW v10 — shared UI components
│       └── NotificationsDropdown.vue  # Bell icon + notification list (all layouts)
├── composables/
│   └── useVoiceRecorder.js  # MediaRecorder wrapper (start/stop/upload/playback)
├── layouts/
│   ├── AdminLayout.vue       # Sidebar + Topbar — bell icon added (v10)
│   ├── TeacherLayout.vue     # Bell icon added (v10)
│   ├── LibrarianLayout.vue
│   ├── SuperLayout.vue
│   └── ParentLayout.vue
├── lib/
│   └── supabase.js          # Supabase client init
├── router/
│   └── index.js             # Vue Router + route guards (39 named routes)
├── stores/
│   ├── auth.js              # Pinia: session, profile, teacherProfile, role, login/logout/init
│   ├── academicYear.js      # Pinia: selectedYearId, selectedYearName — persisted
│   ├── school.js            # Pinia: schoolId, schoolName, schoolCode — persisted
│   ├── classSubjects.js     # Pinia: subjects cached by classId
│   └── notifications.js     # NEW v10 — Pinia: unread count, notification list, mark-read
├── views/
│   ├── auth/
│   │   ├── LoginView.vue
│   │   ├── RegisterView.vue
│   │   └── UnauthorizedView.vue
│   ├── admin/               # 21 views (ApprovalView.vue added in v10)
│   ├── teacher/             # 18 views
│   ├── librarian/           # 4 views
│   ├── super/               # 3 views
│   └── parent/              # 2 views (link-only portal)
├── utils/
│   ├── scoreCalculator.js   # computeMonthlyAverage, computeSemesterAverage, computeRank
│   ├── formatDate.js
│   └── exportPdf.js         # generateMonthlyScorePDF, generateSemesterScorePDF
├── App.vue
└── main.js
```

**Total source files: ~73** (51 views, 7 components, 1 composable, 5 stores, 3 utils, 5 layouts, 1 router)

---

## 🗺️ Pages by Role

### 🔐 Standalone Pages (NO layout)

| Route                   | Page                                                                              |
| ----------------------- | --------------------------------------------------------------------------------- |
| `/`                     | Landing page — app overview, features, schools using it, contact                  |
| `/login`                | Login page (all roles — email + password only, system auto-detects role + school) |
| `/register`             | Self-registration page (select school, enter role)                                |
| `/unauthorized`         | No access page                                                                    |
| `/admin/academic-years` | **Admin only.** Academic year selector — Layer 1 entry point                      |

---

### 🦸 Super Admin Pages `/super` (Tra only)

| Route                | Page                                                                        |
| -------------------- | --------------------------------------------------------------------------- |
| `/super/dashboard`   | Platform-level overview — total schools, students, teachers across platform |
| `/super/schools`     | Schools list — create, edit, activate/deactivate schools                    |
| `/super/schools/new` | Create new school + first admin account (calls Edge Function)               |
| `/super/users`       | User management (reuses admin UsersView)                                    |
| `/super/settings`    | Settings (reuses admin SettingsView)                                        |

---

### 👨‍💼 Admin Pages `/admin`

#### Layer 1 — Academic Year Page (Standalone, no layout)

| Route                   | Page                   | Notes                                                                                                                               |
| ----------------------- | ---------------------- | ----------------------------------------------------------------------------------------------------------------------------------- |
| `/admin/academic-years` | Academic year selector | CRUD academic years. Click **"មើល"** → saves `academic_year_id` to Pinia → navigates to `/admin/dashboard`. Has **Student Rollup** button. |

#### Layer 2 — Inside AdminLayout (sidebar + topbar)

| Route                        | Page                                                                     |
| ---------------------------- | ------------------------------------------------------------------------ |
| `/admin/dashboard`           | Year-scoped stats (students, teachers, classes, books, budget)           |
| `/admin/settings`            | Tabbed: school info (**+ signature/stamp upload v10**), academic years, subjects, holidays, attendance cfg |
| `/admin/classes`             | Class CRUD + assign teacher/turn/subjects per class                      |
| `/admin/teachers`            | Teacher profile management                                               |
| `/admin/students`            | Student CRUD + bulk Excel import/export + **batch name refactor**        |
| `/admin/students/age`        | Student age distribution report                                          |
| `/admin/students/:id`        | Student detail + health + growth chart + vaccinations + sick days        |
| `/admin/attendance/students` | Student attendance overview by class/date                                |
| `/admin/attendance/teachers` | Teacher attendance overview + admin override                             |
| `/admin/scores`              | Scores overview by class/type/month + stats summary + PDF                |
| `/admin/health`              | Student health records overview                                          |
| `/admin/sick-days`           | Sick days overview + CRUD                                                |
| `/admin/budget`              | Budget transactions (income/expense + running balance)                   |
| `/admin/inventory`           | Inventory items CRUD + stock tracking                                    |
| `/admin/library`             | Library overview (books + borrows, read-only)                            |
| `/admin/users`               | User account management via Edge Function                                |
| `/admin/reports`             | School summary report + PDF                                              |
| `/admin/honor-board-editor`  | Honor board/certificate editor with drag-drop text layers                |
| `/admin/approvals`           | **NEW v10** Report link approval queue — approve or reject pending teacher requests |

---

### 👩‍🏫 Teacher Pages `/teacher`

| Route                                | Page                                                                                     |
| ------------------------------------ | ---------------------------------------------------------------------------------------- |
| `/teacher/dashboard`                 | Class overview + teacher check-in + today's attendance stats                             |
| `/teacher/students`                  | Their class students list                                                                |
| `/teacher/students/:id`              | Student detail + health + growth + vaccinations + sick days                              |
| `/teacher/attendance`                | Mark daily student attendance (bulk)                                                     |
| `/teacher/attendance/my`             | Own attendance — monthly calendar view                                                   |
| `/teacher/scores`                    | Score hub — choose monthly or semester mode                                              |
| `/teacher/scores/monthly`            | Monthly score entry grid + dynamic subjects + averages + rank + PDF                      |
| `/teacher/scores/semester`           | Semester score entry + monthly averages pulled + semester calc + PDF                     |
| `/teacher/scores/ranking`            | Ranked list + stats summary tiles + grade distribution + **Request Approval / Share Link button (v10)** |
| `/teacher/scores/summary/:id`        | Individual student score summary with radar chart (Chart.js)                             |
| `/teacher/scores/certificates`       | Certificate design for Top 5 — template selection + download as image/PDF                |
| `/teacher/scores/report-replies`     | Parent replies inbox — filter by month/semester, read-only text + voice playback + **PhrasePicker chips (v10)** |
| `/teacher/scores/honor-board-editor` | Honor board editor with drag-drop text layers on templates                               |
| `/teacher/sick-days`                 | Add & manage student sick days                                                           |
| `/teacher/growth`                    | Add & view student growth records (height/weight)                                        |
| `/teacher/vaccinations`              | View & manage student vaccination records                                                |
| `/teacher/holidays`                  | View school holidays (read only)                                                         |
| `/teacher/reports`                   | Print class attendance & score reports + PDF                                             |

---

### 📚 Librarian Pages `/librarian`

| Route                  | Page                    |
| ---------------------- | ----------------------- |
| `/librarian/dashboard` | Library stats overview  |
| `/librarian/books`     | Book CRUD               |
| `/librarian/borrows`   | Issue & return books    |
| `/librarian/overdue`   | Overdue books list      |

---

### 👨‍👩‍👧 Parent Portal `/parent` (anon — link-only, no login)

| Route                                        | Page           | Notes                                                                                            |
| -------------------------------------------- | -------------- | ------------------------------------------------------------------------------------------------ |
| `/parent/report/:report_link_id`             | Class dropdown | Parent selects their child from the dropdown                                                     |
| `/parent/report/:report_link_id/:student_id` | Report card    | Scores + attendance + teacher message (text/voice) + parent reply (text + voice recorder) + **principal signature + school stamp at footer (v10, approved links only)** |

**Removed routes (old v13 search-based portal):** `/parent`, `/parent/student/:id/*` — all removed. Health data never exposed to anon.

---

## 🗄️ Schema & Data Model (v10)

### Core Tables

| Table                  | Purpose                                              |
| ---------------------- | ---------------------------------------------------- |
| `schools`              | Multi-tenant: one row per school                     |
| `users`                | Linked to `auth.users`; `school_id` scopes tenant    |
| `teachers`             | Profile for ALL staff (admin/teacher/librarian)      |
| `school_settings`      | Late thresholds, shift times per school              |
| `school_information`   | School profile — **v10 adds `signature_url`, `stamp_url`** |
| `academic_years`       | Per-school academic years                            |
| `subjects`             | Global subject pool per school                       |
| `classes`              | Per-school + per-year, assigned teacher + turn       |
| `class_subjects`       | Junction: which subjects does each class have        |
| `students`             | Core student data, scoped to class + year            |
| `student_health`       | Static health profile (staff only)                   |
| `student_checkups`     | Periodic health visits (staff only)                  |
| `student_growth`       | Height/weight records (staff only)                   |
| `student_vaccinations` | Vaccination records (staff only)                     |
| `student_sick_days`    | Sick day records (staff only)                        |
| `attendances`          | Daily student attendance (anon scoped to report link)|
| `scores`               | Raw subject scores per student (anon scoped)         |
| `teacher_attendances`  | Teacher check-in (once-per-day, auto-calc late)      |
| `school_holidays`      | Off days shaded in attendance calendars              |
| `books`                | Library books                                        |
| `book_borrows`         | Book borrow records                                  |
| `budget_transactions`  | Income/expense tracking                              |
| `inventory_items`      | School inventory                                     |
| `report_links`         | UUID tokens per class per month/semester — **v10 adds `status`, `rejection_note`, `approved_at`, `approved_by`** |
| `report_messages`      | Teacher message + parent reply per student           |
| `notifications`        | **NEW v10** In-app notifications for teacher + admin |
| `teacher_phrases`      | **NEW v10** Personal reusable feedback phrases per teacher |
| `semester_config`      | Semester month ranges per school per academic year   |

### Storage Buckets

| Bucket              | Purpose                                                        |
| ------------------- | -------------------------------------------------------------- |
| `teacher-profiles`  | Staff profile pictures (public read)                           |
| `report-voices`     | Teacher/parent voice `.webm` files                             |
| `school-assets`     | **NEW v10** Principal signature + school stamp images (public read) |

### Key Indexes
- 25+ indexes for multi-tenant performance (all tables indexed by `school_id`)
- Composite indexes on attendance, scores, report_links for common queries
- **v10:** indexes on `notifications(recipient_user_id, is_read)`, `teacher_phrases(teacher_id)`, `report_links(status, school_id)`

---

## 📋 Phase Tracker

### ✅ Phase 1 — Foundation
- [x] Supabase project setup
- [x] Auth (email + password)
- [x] Role-based routing + route guards
- [x] School settings

### ✅ Phase 2 — Admin Core
- [x] Academic year management (Layer 1 standalone page)
- [x] Class + subject management (per-class via `class_subjects`)
- [x] Teacher management
- [x] Student management + bulk Excel import
- [x] School information page

### ✅ Phase 3 — Admin Extended
- [x] Student attendance overview
- [x] Teacher attendance overview + admin override
- [x] Score overview with stats
- [x] Budget management with running balance
- [x] Inventory management with low-stock alerts
- [x] School holidays
- [x] Library overview (read-only)
- [x] User management (via Edge Function)
- [x] **Student Rollup UI** — rollup modal on Academic Years page

### ✅ Phase 4 — Teacher Features
- [x] Dashboard with teacher check-in
- [x] Class & students management
- [x] Student attendance (bulk mark + monthly calendar)
- [x] Teacher own attendance (monthly calendar view)
- [x] Monthly score entry (dynamic subjects + average + rank + PDF)
- [x] Semester score entry (monthly averages pulled + semester calc + PDF)
- [x] Health & wellness (sick days, growth, vaccinations)
- [x] Score ranking with stats summary tiles + grade distribution
- [x] Reports with PDF generation
- [x] Certificate design (Top 5 award certificates)
- [x] Honor board editor (drag-drop text layers on templates)
- [x] **Score summary with radar chart** (individual student view)

### ✅ Phase 5 — Librarian Features
- [x] Dashboard
- [x] Book CRUD
- [x] Issue & return books
- [x] Overdue list

### ✅ Phase 6 — Old Parent Portal (v13, superseded)
- [x] Search by name + DOB → **removed in v14**
- [x] Student overview → removed
- [x] Health/growth/vaccinations tabs → removed (private)

### ✅ Phase 7 — Student Rollup (v7)
- [x] `perform_student_rollup()` DB function
- [x] `clone_classes_structure()` DB function
- [x] Khmer digit parsing (`khmer_to_int`, `extract_grade_from_class_name`)
- [x] Grade 6 → `is_graduated = true` handling

### ✅ Phase 8 — Stabilize & Polish ← IN TESTING
- [ ] Complete team testing at Battambang school
- [ ] Fix bugs from real user feedback
- [ ] Teacher training
- [ ] Loading states & skeleton loaders (partial)
- [ ] Empty states (partial)
- [ ] Toast notifications (exists globally, needs consistency check)
- [ ] Confirm dialogs (partial)
- [ ] Form validation (vee-validate + yup installed, usage inconsistent)
- [ ] Mobile/tablet responsive polish
- [ ] Deploy to Vercel + monitor via Supabase dashboard

### ✅ Phase 9 — Multi-Tenant Architecture (v8) ← DONE
- [x] `schools` table + `school_id` on all tables
- [x] `super_admin` role + bypass RLS
- [x] `get_user_school_id()` helper + updated RLS policies
- [x] 25+ performance indexes
- [x] Landing page (`/`) — HomeView.vue
- [x] Super admin dashboard + school management (`/super/*`)
- [x] Pinia stores updated (`auth.js`, `school.js`, `academicYear.js`)
- [x] Login auto-detects school from user record
- [x] ClassSubjects store with memoization
- [ ] Update Edge Function `manage-user` with `school_id` payload
- [ ] Multi-tenant testing (2 schools, verify isolation)

### ✅ Phase 10 — Link-Based Parent Portal (v9) ← DONE (frontend built)
- [x] Migration SQL (`migration_v8_v9.sql`) ready
- [x] `report-voices` storage bucket + policies in schema
- [x] Generate Report Link button on `/teacher/scores/ranking`
- [x] `/teacher/scores/report-replies` — parent replies inbox
- [x] `/parent/report/:report_link_id` — class dropdown view
- [x] `/parent/report/:report_link_id/:student_id` — report card + messaging
- [x] `useVoiceRecorder.js` composable (MediaRecorder wrapper)
- [x] Teacher voice message upload + playback
- [x] Parent text reply + voice reply
- [x] Old v13 parent routes removed from router
- [x] Anon RLS tightened (health data private, students/scores/attendances scoped)
- [x] Missing anon policies added (`classes`, `subjects` for `!inner` joins)
- [ ] End-to-end testing: link sharing, voice recording, cross-school isolation

### 🔧 Phase 11 — Approval Flow + Notifications + Phrase Library (v10) ← CURRENT
#### Schema / Backend
- [ ] `migration_v9_v10.sql` applied to production
- [ ] `report_links` — add `status`, `rejection_note`, `approved_at`, `approved_by`
- [ ] `notifications` table — `recipient_user_id`, `type`, `payload` (jsonb), `is_read`
- [ ] `teacher_phrases` table — `teacher_id`, `phrase_text`, `sort_order`
- [ ] `school_information` — add `signature_url`, `stamp_url`
- [ ] `school-assets` storage bucket + RLS policies
- [ ] `notify_teacher_on_approval()` trigger — fires after `report_links.status` update
- [ ] `notify_admin_on_request()` trigger — fires after `report_links` insert with `status = pending`
- [ ] RLS: notifications (self-read, admin write for teacher notifs)
- [ ] RLS: teacher_phrases (teacher owns their own rows only)
- [ ] RLS: report_links updated — anon blocked from `pending` links

#### Frontend
- [ ] `notifications.js` Pinia store — fetch unread, mark read, realtime subscription
- [ ] `NotificationsDropdown.vue` — bell icon + badge + dropdown list (AdminLayout + TeacherLayout)
- [ ] `PhrasePicker.vue` — chip list + append-on-click + add/delete phrase inline
- [ ] `/admin/approvals` — ApprovalView: pending requests list + approve/reject modal with note
- [ ] `/admin/settings` — School Information tab: add signature upload + stamp upload fields
- [ ] `/teacher/scores/ranking` — button states: `Request Approval` → `⏳ Pending…` → `✅ Share` / `❌ Rejected (reason)`
- [ ] `/parent/report/:report_link_id/:student_id` — footer: signature + stamp display (approved links only)
- [ ] `report-replies` view — integrate `PhrasePicker.vue` below teacher message textarea
- [ ] `school-assets` bucket wired to signature/stamp upload inputs

---

## 🔧 Bugs Fixed During v15

- [x] Male student avatar white-on-white in `ScoresRankingView` — changed `var(--primary-color)` to `#3b82f6`
- [x] Same fix in `ReportRepliesView` for male student avatars
- [x] Report link null check — `.eq('semester', null)` replaced with `.is('semester', null)`
- [x] Duplicate report links in dropdown — deduplicated by unique key
- [x] Action button in ranking table now routes to working ScoreSummary page instead of dead `/teacher/scores/view`

---

## 🆕 Features Added in v15

- **ScoreSummaryView** (`/teacher/scores/summary/:id`) — radar/spiderweb chart showing student scores across subjects using Chart.js + vue-chartjs, with score table below
- **Batch name refactor** in admin StudentsView — swaps "Last First" → "First Last" for selected or all students, with progress bar modal

---

## 🆕 Features Added in v16 (schema v10)

- **Report Link Approval Flow** — teacher requests approval → admin approves/rejects → link unlocks → parent report card shows principal signature + school stamp
- **In-App Notifications** — bell icon in Admin + Teacher topbars, unread count badge, notification list dropdown, Supabase Realtime subscription
- **Teacher Phrase Library** (`teacher_phrases`) — personal reusable feedback chips that append to the student message box in report-replies; add/delete inline
- **school-assets bucket** — stores principal digital signature and school stamp images uploaded from Admin Settings

---

## 📦 Dependencies

| Package              | Version | Purpose                    |
| -------------------- | ------- | -------------------------- |
| vue                  | ^3.5    | Framework                  |
| vue-router           | ^5.0    | Routing                    |
| pinia                | ^3.0    | State management           |
| @supabase/supabase-js| ^2.105  | Supabase client            |
| chart.js + vue-chartjs| ^4.5/^5.3 | Charts (radar, etc.)     |
| jspdf + jspdf-autotable| ^4.2/^5.0 | PDF generation          |
| html2canvas          | ^1.4    | Certificate image capture   |
| xlsx                 | ^0.18   | Excel import/export        |
| @heroicons/vue       | ^2.2    | Icons                      |
| vee-validate + yup   | ^4.15/^1.7 | Form validation          |
| @vueuse/core         | ^14.2   | Composition utilities      |
| tailwindcss          | ^4.2    | CSS utility framework      |
| vite                 | ^8.0    | Build tool                 |

---

## 🚀 Known Gaps / Next Actions

1. **Phase 11 frontend** — build `NotificationsDropdown.vue`, `PhrasePicker.vue`, `ApprovalView.vue`, update ranking + report-replies views
2. **school-assets bucket** — wire signature/stamp upload to Admin Settings → School Information tab
3. **Parent report card footer** — render signature + stamp only on approved links
4. **Edge Function `manage-user`** — needs `school_id` payload verification
5. **Multi-tenant testing** — verify data isolation between 2 schools
6. **Parent portal E2E** — test full flow: generate link → request approval → admin approves → share → parent views → voice reply
7. **Validation** — vee-validate + yup installed but not consistently used across forms
8. **Responsive polish** — tablet/mobile layouts need attention
9. **Empty/loading states** — skeleton loaders exist in some views, missing in others
10. **Toast consistency** — global toast styles exist, but usage pattern varies across views

---

## 📌 Notes

- Supabase anon key is safe to expose in frontend (RLS protects data)
- Parent portal uses Supabase anon role — no login required, link-scoped only
- **Anon users cannot access `pending` or `rejected` report links (v10 RLS)**
- Score calculations happen on the frontend (`scoreCalculator.js`) — DB stores raw scores only
- Rank computed within class only — ties share rank, next rank skips (1,1,3...)
- Student rollup parses grade from class name — requires consistent `ថ្នាក់ទី{Khmer digit}{letter}` naming
- Grade 6 graduates: `is_graduated = true`, `class_id = null`
- Subjects are per-class — fetched from `class_subjects`, not global `subjects`
- Grade 1-3 no English · Grade 4-6 includes English
- Cambodia school days = **Monday to Saturday** — Sundays off by default
- `manage-user` Edge Function always creates 3 rows: `auth.users` + `public.users` + `teachers`
- Voice messages stored as `.webm` in `report-voices` bucket
- Signature + stamp images stored in `school-assets` bucket (public read, admin write only)
- Notifications use Supabase Realtime channel — subscribe on login, unsubscribe on logout
- Teacher phrases are soft-ordered by `sort_order` (integer) — allows manual reordering in future
- Potential partnerships: MoEYS, UNICEF Cambodia, World Bank, Room to Read, Aide et Action
